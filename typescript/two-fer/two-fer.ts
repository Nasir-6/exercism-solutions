/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(name:string = "you"): string {
  // Let default name be "you" so can avoid a conditional!

  return `One for ${name}, one for me.`;  
}
